apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "almost-homers-app.fullname" . }}-test-connection"
  labels:
    {{- include "almost-homers-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "almost-homers-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
