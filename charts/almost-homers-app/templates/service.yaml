apiVersion: v1
kind: Service
metadata:
  name: {{ include "almost-homers-app.fullname" . }}
  labels:
    {{- include "almost-homers-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "almost-homers-app.selectorLabels" . | nindent 4 }}
